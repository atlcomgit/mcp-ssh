# Разрешённые хосты (через запятую). Если пусто — ограничений нет.
MCP_SSH_ALLOWED_HOSTS=example.com,10.0.0.10

# Дефолтные параметры подключения (необязательно)
# Хост удалённого сервера
MCP_SSH_DEFAULT_HOST=example.com
# Порт SSH (по умолчанию 22)
MCP_SSH_DEFAULT_PORT=22
# Имя пользователя SSH
MCP_SSH_DEFAULT_USERNAME=
# Путь к приватному ключу SSH (поддерживается ~)
MCP_SSH_DEFAULT_PRIVATE_KEY_PATH=
# Пароль SSH (если не используется ключ)
MCP_SSH_DEFAULT_PASSWORD=
# Команда по умолчанию, если MCP не передаёт параметры
MCP_SSH_DEFAULT_COMMAND=echo "Не передана команда в ssh"

# Общие настройки
# Таймаут подключения (мс)
MCP_SSH_CONNECT_TIMEOUT_MS=20000
# Таймаут выполнения команды (мс)
MCP_SSH_EXEC_TIMEOUT_MS=60000

# Путь к файлу логов команд и результатов
MCP_SSH_LOG_PATH=
